import LinearGradient from "react-native-linear-gradient";
import styled from "styled-components";

export const Container = styled(LinearGradient).attrs({
    colors: ["#0277BD", "#757575"]
})`
   flex: 1;
`